Observação: os comentários excessivos são apenas para explicar o raciocínio e os passos, não costumo comentar excessivamente assim, justamente por deixar o código bagunçado e poluído.

Dependências:
Dispostas no requirements.txt
Pandas (pip install pandas)
Re (módulo do python)

Como executar:
- Não foi feita nenhuma alteração de nome após o download e os arquivos devem estar no mesmo diretório do script (caminho relativo);
- Certifique-se que os arquivos estão com o nome, formato e local corretos;
- Certifique-se também que as dependências estão devidamente instaladas;
- Executar no terminal (ou em alguma IDE, compilador, etc) os arquivos etapa_1.py e etapa_2.py (utilizando o comando: python etapa_1.py).

Nome e formato dos arquivos:
Tarefa 1: Vendas - Vendas.csv
Tarefa 2: Vendas - Pagamentos.csv
Etapa 2: Partnership.txt

Resultado esperado:
Se os passos foram seguidos corretamente, o resultado esperado para a etapa_1 é gerar dois arquivos .csv com as tabelas solicitadas, cumprindo os requisitos das duas tarefas.
O arquivo etapa_2 também retorna uma planilha com os dados solicitados na tarefa.
Em ambos os scripts é exibido o resultado no terminal usando o print (apenas para exibir o resultado, não faria isso normalmente).

Resolução:
Sobre a resolução, acredito que não havia necessidade de usar orientação a objetos ou algum outro conceito complexo de programação, tentei deixar o código o mais simples possível.
Um adendo na segunda etapa seria que eu encontro os dados devido à palavra CPF, seria possível fazer utilizando cotas mas essa palavra aparecia em outras frases, e eu teria que deixar o código mais robusto para lidar com essas frases.
Os nomes infelizmente dão problema no codificador de caracteres.